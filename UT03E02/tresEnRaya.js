let tablero = [
    ["", "", ""],
    ["", "", ""],
    ["", "", ""]
]
let movimiento = prompt("Escribe el numero de casilla que quieres marcar");
let turno = 0;

hacerMov(movimiento);

const hacerMov = (mov) => {
    if (turno % 2===0) {

    }
    switch (mov) {
        case mov<3:
            tablero.push();
            break;
    }
    turno++;
}